{
  "name": "@adhd-db/web",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && cp .assetsignore dist/ && npx wrangler deploy",
    "astro": "astro"
  },
  "dependencies": {
    "@adhd-db/data": "workspace:*",
    "@adhd-db/meta": "workspace:*",
    "@adhd-db/schema": "workspace:*",
    "@astrojs/cloudflare": "^12.6.12",
    "astro": "^5.16.3",
    "yaml": "^2.6.0"
  }
}