---
import { languages, type Lang } from '../i18n/translations';
import { getLangFromUrl, useTranslations, getLocalizedPath, getAlternateLinks } from '../i18n/utils';
import '../styles/tokens.css';

interface Props {
  title: string;
}

const { title } = Astro.props;
const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const alternates = getAlternateLinks(Astro.url.pathname, lang);
---

<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <!-- Google Analytics -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-SJNPNV5QVR"></script>
    <script is:inline>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-SJNPNV5QVR');
    </script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Cormorant+Garamond:wght@400;500;600;700&family=DM+Mono:wght@400;500&display=swap" rel="stylesheet" />
    <meta name="generator" content={Astro.generator} />
    <title>{title} | ADHD Database</title>
    {alternates.map(({ lang: l, path }) => (
      <link rel="alternate" hreflang={l} href={path} />
    ))}
  </head>
  <body>
    <aside class="disclaimer">
      {t('disclaimer')}
    </aside>
    <header>
      <nav>
        <div class="nav-links">
          <a href={getLocalizedPath('/', lang)}>{t('nav.home')}</a>
          <a href={getLocalizedPath('/schema', lang)}>{t('nav.schema')}</a>
          <a href={getLocalizedPath('/meta', lang)}>{t('nav.meta')}</a>
          <a href={getLocalizedPath('/data', lang)}>{t('nav.data')}</a>
          <a href={getLocalizedPath('/tools/region-checker', lang)}>{t('nav.tools')}</a>
        </div>
        <div class="lang-switcher">
          {alternates.map(({ lang: l, path }) => (
            <a href={path} class:list={[{ active: l === lang }]}>{languages[l]}</a>
          ))}
        </div>
      </nav>
    </header>
    <main>
      <slot />
    </main>
    <footer>
      <p>
        {t('footer.maintainedBy')} <a href="https://yaluotao.me" target="_blank" rel="noopener">yaluotao</a>
        &nbsp;·&nbsp;
        {t('footer.contributions')} <a href="https://github.com/ya-luotao/adhd-db" target="_blank" rel="noopener">GitHub</a>
      </p>
    </footer>
  </body>
</html>

<style is:global>
  /* ============================================
   * BASE RESET & DEFAULTS
   * ============================================ */

  * {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  html {
    font-family: var(--font-sans);
    font-size: 16px;
    line-height: var(--leading-normal);
    background: var(--color-bg-secondary);
    color: var(--color-text-secondary);
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  body {
    max-width: var(--container-max);
    margin: 0 auto;
    padding: var(--space-4);
  }

  /* ============================================
   * TYPOGRAPHY
   * ============================================ */

  h1, h2, h3, h4, h5, h6 {
    color: var(--color-text-primary);
    font-weight: var(--font-semibold);
    line-height: var(--leading-tight);
    letter-spacing: var(--tracking-tight);
  }

  h1 {
    font-size: var(--text-3xl);
    font-weight: var(--font-bold);
    margin-bottom: var(--space-6);
  }

  h2 {
    font-size: var(--text-2xl);
    margin: var(--space-8) 0 var(--space-4);
  }

  h3 {
    font-size: var(--text-xl);
    margin: var(--space-6) 0 var(--space-3);
  }

  h4 {
    font-size: var(--text-lg);
    margin: var(--space-4) 0 var(--space-2);
  }

  p {
    margin-bottom: var(--space-4);
  }

  a {
    color: var(--color-primary);
    text-decoration: none;
    transition: color var(--transition-fast);
  }

  a:hover {
    color: var(--color-primary-hover);
    text-decoration: underline;
  }

  a:focus-visible {
    outline: 2px solid var(--color-primary);
    outline-offset: 2px;
    border-radius: var(--radius-sm);
  }

  /* ============================================
   * DISCLAIMER BANNER
   * ============================================ */

  .disclaimer {
    background: var(--color-warning-light);
    border: 1px solid var(--color-warning);
    border-radius: var(--radius-md);
    padding: var(--space-3) var(--space-4);
    margin-bottom: var(--space-4);
    font-size: var(--text-sm);
    color: var(--color-warning-700);
    text-align: center;
    line-height: var(--leading-relaxed);
  }

  /* ============================================
   * HEADER & NAVIGATION
   * ============================================ */

  header {
    margin-bottom: var(--space-8);
  }

  nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: var(--space-4) 0;
    border-bottom: 1px solid var(--color-border);
  }

  .nav-links {
    display: flex;
    gap: var(--space-6);
  }

  .nav-links a {
    color: var(--color-text-secondary);
    text-decoration: none;
    font-weight: var(--font-medium);
    font-size: var(--text-sm);
    padding: var(--space-2) 0;
    position: relative;
    transition: color var(--transition-fast);
  }

  .nav-links a:hover {
    color: var(--color-primary);
    text-decoration: none;
  }

  .nav-links a::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 2px;
    background: var(--color-primary);
    transition: width var(--transition-base);
  }

  .nav-links a:hover::after {
    width: 100%;
  }

  .lang-switcher {
    display: flex;
    gap: var(--space-1);
    background: var(--color-bg-tertiary);
    padding: var(--space-1);
    border-radius: var(--radius-md);
  }

  .lang-switcher a {
    padding: var(--space-1-5) var(--space-3);
    border-radius: var(--radius-sm);
    text-decoration: none;
    color: var(--color-text-tertiary);
    font-size: var(--text-sm);
    font-weight: var(--font-medium);
    transition: all var(--transition-fast);
  }

  .lang-switcher a:hover {
    color: var(--color-text-primary);
    background: var(--color-bg-primary);
  }

  .lang-switcher a.active {
    background: var(--color-bg-primary);
    color: var(--color-primary);
    box-shadow: var(--shadow-xs);
  }

  /* ============================================
   * MAIN CONTENT
   * ============================================ */

  main {
    min-height: calc(100vh - 200px);
  }

  /* ============================================
   * FOOTER
   * ============================================ */

  footer {
    margin-top: var(--space-16);
    padding: var(--space-6) 0;
    border-top: 1px solid var(--color-border);
    text-align: center;
    font-size: var(--text-sm);
    color: var(--color-text-tertiary);
  }

  footer a {
    color: var(--color-text-secondary);
    font-weight: var(--font-medium);
  }

  footer a:hover {
    color: var(--color-primary);
  }

  /* ============================================
   * CODE BLOCKS
   * ============================================ */

  pre {
    background: var(--color-gray-900);
    color: var(--color-gray-100);
    padding: var(--space-4);
    border-radius: var(--radius-lg);
    overflow-x: auto;
    font-size: var(--text-sm);
    line-height: var(--leading-relaxed);
    border: 1px solid var(--color-gray-800);
  }

  code {
    font-family: var(--font-mono);
    font-size: 0.9em;
  }

  :not(pre) > code {
    background: var(--color-bg-tertiary);
    padding: var(--space-0-5) var(--space-1-5);
    border-radius: var(--radius-sm);
    color: var(--color-text-primary);
  }

  /* ============================================
   * CARDS
   * ============================================ */

  .card {
    background: var(--color-bg-primary);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: var(--space-6);
    margin-bottom: var(--space-4);
    transition: box-shadow var(--transition-base), transform var(--transition-base);
  }

  .card:hover {
    box-shadow: var(--shadow-md);
  }

  .card h3 {
    margin-top: 0;
  }

  a.card {
    display: block;
    color: inherit;
    text-decoration: none;
  }

  a.card:hover {
    transform: translateY(-2px);
    text-decoration: none;
  }

  /* ============================================
   * GRID
   * ============================================ */

  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: var(--space-4);
  }

  /* ============================================
   * TAGS / BADGES
   * ============================================ */

  .tag {
    display: inline-flex;
    align-items: center;
    padding: var(--space-1) var(--space-2-5);
    border-radius: var(--radius-full);
    font-size: var(--text-xs);
    font-weight: var(--font-medium);
    margin-right: var(--space-2);
    line-height: var(--leading-none);
  }

  .tag.stimulant {
    background: var(--color-stimulant-bg);
    color: var(--color-stimulant-text);
  }

  .tag.non-stimulant {
    background: var(--color-non-stimulant-bg);
    color: var(--color-non-stimulant-text);
  }

  .tag.available {
    background: var(--color-available-bg);
    color: var(--color-available-text);
  }

  .tag.unavailable {
    background: var(--color-unavailable-bg);
    color: var(--color-unavailable-text);
  }

  /* ============================================
   * TABLES
   * ============================================ */

  table {
    width: 100%;
    border-collapse: collapse;
    margin: var(--space-4) 0;
    font-size: var(--text-sm);
  }

  th, td {
    padding: var(--space-3) var(--space-4);
    text-align: left;
    border-bottom: 1px solid var(--color-border);
  }

  th {
    background: var(--color-bg-secondary);
    font-weight: var(--font-semibold);
    color: var(--color-text-primary);
    font-size: var(--text-xs);
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
  }

  tr:hover {
    background: var(--color-bg-secondary);
  }

  /* ============================================
   * FORM ELEMENTS
   * ============================================ */

  input, select, textarea {
    font-family: inherit;
    font-size: var(--text-sm);
  }

  input[type="text"],
  input[type="search"],
  input[type="email"],
  select {
    width: 100%;
    padding: var(--space-2-5) var(--space-3);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    background: var(--color-bg-primary);
    color: var(--color-text-primary);
    transition: border-color var(--transition-fast), box-shadow var(--transition-fast);
  }

  input:focus,
  select:focus,
  textarea:focus {
    outline: none;
    border-color: var(--color-primary);
    box-shadow: var(--shadow-focus);
  }

  input::placeholder {
    color: var(--color-text-muted);
  }

  /* ============================================
   * BUTTONS
   * ============================================ */

  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-2);
    padding: var(--space-2-5) var(--space-4);
    border: none;
    border-radius: var(--radius-md);
    font-size: var(--text-sm);
    font-weight: var(--font-medium);
    cursor: pointer;
    transition: all var(--transition-fast);
    text-decoration: none;
  }

  .btn:focus-visible {
    outline: none;
    box-shadow: var(--shadow-focus);
  }

  .btn-primary {
    background: var(--color-primary);
    color: var(--color-text-inverse);
  }

  .btn-primary:hover {
    background: var(--color-primary-hover);
    text-decoration: none;
  }

  .btn-secondary {
    background: var(--color-bg-primary);
    color: var(--color-text-primary);
    border: 1px solid var(--color-border);
  }

  .btn-secondary:hover {
    background: var(--color-bg-secondary);
    border-color: var(--color-border-dark);
    text-decoration: none;
  }

  /* ============================================
   * UTILITY CLASSES
   * ============================================ */

  .text-muted {
    color: var(--color-text-tertiary);
  }

  .text-sm {
    font-size: var(--text-sm);
  }

  .font-medium {
    font-weight: var(--font-medium);
  }

  .mt-0 { margin-top: 0; }
  .mb-0 { margin-bottom: 0; }

  /* ============================================
   * RESPONSIVE - MOBILE FIRST
   * ============================================ */

  /* Mobile: < 640px */
  @media (max-width: 639px) {
    body {
      padding: var(--space-3);
    }

    h1 {
      font-size: var(--text-2xl);
      margin-bottom: var(--space-4);
    }

    h2 {
      font-size: var(--text-xl);
      margin: var(--space-6) 0 var(--space-3);
    }

    h3 {
      font-size: var(--text-lg);
    }

    /* Navigation stacks vertically on mobile */
    nav {
      flex-direction: column;
      gap: var(--space-3);
      padding: var(--space-3) 0;
    }

    .nav-links {
      width: 100%;
      justify-content: center;
      gap: var(--space-4);
      flex-wrap: wrap;
    }

    .nav-links a {
      font-size: var(--text-sm);
    }

    .lang-switcher {
      width: 100%;
      justify-content: center;
    }

    /* Cards get less padding on mobile */
    .card {
      padding: var(--space-4);
    }

    /* Grid becomes single column */
    .grid {
      grid-template-columns: 1fr;
      gap: var(--space-3);
    }

    /* Tables become scrollable */
    table {
      display: block;
      overflow-x: auto;
      -webkit-overflow-scrolling: touch;
    }

    th, td {
      padding: var(--space-2) var(--space-3);
      white-space: nowrap;
    }

    /* Smaller disclaimer text */
    .disclaimer {
      font-size: var(--text-xs);
      padding: var(--space-2) var(--space-3);
    }

    /* Footer adjustments */
    footer {
      margin-top: var(--space-10);
      padding: var(--space-4) 0;
    }

    /* Code blocks */
    pre {
      padding: var(--space-3);
      font-size: var(--text-xs);
      border-radius: var(--radius-md);
    }

    /* Tags wrap better */
    .tag {
      margin-bottom: var(--space-1);
    }
  }

  /* Tablet: 640px - 1023px */
  @media (min-width: 640px) and (max-width: 1023px) {
    .grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  /* Touch-friendly targets */
  @media (pointer: coarse) {
    .nav-links a {
      padding: var(--space-3) var(--space-1);
    }

    .lang-switcher a {
      padding: var(--space-2) var(--space-4);
    }

    .btn {
      padding: var(--space-3) var(--space-5);
      min-height: 44px;
    }

    input[type="text"],
    input[type="search"],
    input[type="email"],
    select {
      padding: var(--space-3);
      min-height: 44px;
    }
  }

  /* Ensure images don't overflow */
  img {
    max-width: 100%;
    height: auto;
  }

  /* Better text wrapping */
  p, li, td {
    overflow-wrap: break-word;
    word-wrap: break-word;
  }
</style>
